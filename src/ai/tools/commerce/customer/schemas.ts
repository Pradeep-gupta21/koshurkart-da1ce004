import type { JSONSchema } from "@/ai/types/chat";

/**
 * Provider-agnostic JSON-Schema definitions for Customer Tools.
 */
export const CustomerProfileSchema: JSONSchema = {
  type: "object",
  description: "Retrieve customer profile details",
  properties: {},
};
